package Firstagain;
import java.math.BigDecimal;
import java.math.BigInteger;
import java.util.Arrays;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        int[] myArr = {1,2,3,4,5,6,7,45,42,12,1,23,4,48};
        First f = new First();
        f.find(myArr);












        String username = "Fusun"; //variable = deyisen.
        /* primitive data types : boolean, char, int, float, double, long
         non-primitive data types (wrapper), class
         sert - if,else if, else.
         loop - while, do while, for.
         array.
         klass. oop1
         encapsulation OOP2
         comparable, comparator.
         July 15 && 16 && 17 && 21 && 22 && 23 tekrar olundu.

         */
    }
}
